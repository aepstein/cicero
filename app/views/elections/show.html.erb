<p>
  <b>Name:</b> <%=h @election.name %>
</p>

<p>
  <b>Voting starts at:</b> <%=h @election.voting_starts_at %>
</p>

<p>
  <b>Voting ends at:</b> <%=h @election.voting_ends_at %>
</p>

<p>
  <b>Results available at:</b> <%=h @election.results_available_at %>
</p>

<p>
  <strong>Contact name:</strong> <%=h @election.contact_name %>
</p>

<p>
	<strong>Contact email:</strong> <%=h @election.contact_email %>
</p>

<p>
  <strong>Verify message:</strong>
</p>
  <%=markdown @election.verify_message %>

<p>
  <b>Ballots cast so far:</b> <%=h @election.ballots.cast_size %><br/>
  <b>Ballots started, not cast:</b> <%=h @election.ballots.uncast_size %>
</p>

<% if @election.may_user?(current_user,:tabulate) %>
<p>
<%= link_to 'Tabulate results',
            tabulate_election_path(@election),
            :method => :post %>
</p>
<% end %>

<h3>Races for this election</h3>

<p>
<%= link_to "Add race for #{@election}",
            new_election_race_path(@election) %>
</p>

<% if @election.races.empty? %>
<p><em>There are no races specified for this election yet.</em></p>
<% else %>
<%= render :partial => 'elections/races/race', :collection => @election.races %>
<% end %>

<p>
<%= link_to "Add roll for #{@election}",
            new_election_roll_path(@election) %>
</p>

<h3>Rolls for this election</h3>

<% if @election.rolls.empty? %>
<p><em>There are no rolls specified for this election yet.</em></p>
<% else %>
<ul>
<%= render :partial => 'elections/rolls/roll', :collection => @election.rolls %>
</ul>
<% end %>

<hr/>
<p>
<%= link_to( 'Edit',
             edit_election_path(@election) ) if @election.may_user?(current_user, :update) %> |
<%= link_to 'Back', elections_path %>
</p>

