<h2>Editing election</h2>

<%= error_messages_for :election %>

<% form_for(@election) do |f| %>

<%= render :partial => 'form',
           :locals => { :f => f } %> 

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<hr/>
<p>
<%= link_to 'Show', @election %> |
<%= link_to 'Back', elections_path %>
</p>