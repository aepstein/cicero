<%=content_tag :h1, "Elections for #{current_user.name}" %>

<% if elections.empty? %>
  <p>
  <em>There are no elections remaining for you to vote in at this time.</em>
  </p>
<% else %>
  <p>
  Please select the election in which you wish to vote:
  </p>
  <%=content_tag :ul, id: "active-elections" do %>
    <%= render partial: 'home/election', collection: elections %>
  <% end %>
<% end %>

<% if current_user.elections.size > 0 %>
  <p>
  You previously voted in these elections:
  </p>
  <%=content_tag :ul, id: "cast-ballots" do %>
    <%= render partial: 'home/ballot', collection: current_user.ballots(include: :election) %>
  <% end %>
<% end %>

