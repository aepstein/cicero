<h2>Editing candidate for <%=h @candidate.race %></h2>

<% semantic_form_for( @candidate, :html => { :multipart => true } ) do |f| %>

<%= render :partial => 'form', :object => f %>

<% end %>

<p>
<%= link_to 'Show', candidate_path( @candidate ) %> |
<%= link_to 'Back', race_candidates_path( @candidate.race ) %>
</p>

