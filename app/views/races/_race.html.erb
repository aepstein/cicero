<tr>
  <td><%= race.name %></td>
  <td><%= race.slots %></td>
  <td><%= race.is_ranked? ? 'Yes' : 'No' %></td>
  <td><%= race.roll %></td>
  <td>
<%= link_to( race.name, race_path( race ) ) if race.may_user? current_user, :show %>
<%= link_to( 'Edit', edit_race_path( race ) ) if race.may_user? current_user, :update %>
<%= link_to( 'Destroy', race_path( race ), :confirm => 'Are you sure?', :method => :delete) if race.may_user? current_user, :delete  %>
  </td>
</tr>

