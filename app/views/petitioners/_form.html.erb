<%= render "shared/error_messages", target: form.object %>

<%= form.input :user_name, as: :autocomplete, label: 'User', value_method: :to_s,
  url: roll_users_path( form.object.candidate.race.roll, format: :json ) %>

<%= form.button :wrapped, cancel: candidate_petitioners_url( form.object.candidate ) %>

